name: gpex
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  maplibre_gl: ^0.16.0
  latlong2: ^0.9.0
  http: ^1.1.0
  geolocator: ^14.0.2
  flutter_polyline_points: ^3.1.0
  shared_preferences: ^2.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/radar_3d.png
    - assets/icon-radar.jpg
    - assets/damier.jpg